#!/usr/bin/env ruby

board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]

display_board(board)

puts "Please enter 1-9: "
input = gets.strip.to_i

index = input_to_index(position)
valid_move?(board, index)
move(board, index)

